import java.util.ArrayList;

/**
 * A Java class to simulate the card game War.
 *
 */
public class War {

    /**
     * Determines the winner of a game of War.
     *
     * @param deck A deck of cards
     * @return     1 if player A wins, -1 if player B wins, 0 if a draw
     */
    public static int findWinner(ArrayList<Integer> deck) {
        return 0;
    }
}
